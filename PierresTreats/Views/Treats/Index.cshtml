@using PierresTreats.Models

<h2>Treats</h2>

@if (Model.Count == 0)
{
  <p>No Treats yet - add some!</p>
}
else
{
  @foreach (Treat treat in Model)
  {
    <h5>@Html.ActionLink($"{treat.TreatName}", "Details", new { id = treat.TreatId }) </h5>
  }
}


<button>@Html.ActionLink("Add a Treat", "Create")</button>